
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Wanderlust </title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
  <style>
    body, html {
      height: 100%;
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
    }

    .landing-hero {
      background: linear-gradient(135deg, #e6d4f9, #f3e9ff);
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      text-decoration: none;
      color: inherit;
    }

    .plane-icon {
      font-size: 5rem;
      color: #fe424d;
      animation: float 2s ease-in-out infinite;
      margin-bottom: 20px;
    }

    .landing-hero h1 {
      font-size: 3.5rem;
      background: linear-gradient(to right, #333, #fe424d);
      -webkit-background-clip: text;
      color: transparent;
      margin-bottom: 10px;
    }

    .landing-hero p {
      font-size: 1.4rem;
      color: #000; /* Black caption */
      margin-bottom: 40px;
    }

    .landing-hero:hover {
      cursor: pointer;
      background: linear-gradient(135deg, #e6d4f9, #ede0ff);
    }

    @keyframes float {
      0% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
      100% { transform: translateY(0); }
    }
  </style>
</head>
<body>

  <a href="/listings" class="landing-hero">
    <i class="fa-solid fa-plane-departure plane-icon"></i>
    <h1>Wanderlust</h1>
    <p>Where Life Meets Travel</p>
  </a>

</body>
</html>
