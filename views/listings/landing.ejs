<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Wanderlust</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
  <style>
    body, html {
      height: 100%;
      font-family: cursive !important;
      margin: 0;
    }

    .landing-hero {
      background: linear-gradient(135deg, #e6d4f9, #f3e9ff);
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      font-family: cursive !important;
      text-decoration: none;
      color: inherit;
    }

    .plane-icon {
      font-size: 5rem;
      color: #fe424d;
      animation: float 2s ease-in-out infinite;
      margin-bottom: 20px;
    }

    .landing-hero h1 {
      font-size: 3.5rem;
      background: linear-gradient(to right, #333, #fe424d);
      -webkit-background-clip: text;
      color: transparent;
      margin-bottom: 10px;
    }

    .landing-hero p {
      font-size: 1.4rem;
      font-family: cursive !important;
      color: #000;
      margin-bottom: 40px;
    }

    .cta-button {
      font-family: cursive !important;
      font-size: 1.1rem;
      background: linear-gradient(to right, #fe424d, #ff7178);
      border: none;
      color: white;
      padding: 12px 28px;
      border-radius: 30px;
      cursor: pointer;
      transition: background 0.3s ease, transform 0.2s ease;
      text-decoration: none;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }

    .cta-button:hover {
      background: linear-gradient(to right, #ff7178, #fe424d);
      transform: scale(1.05);
    }

    .landing-hero:hover {
      background: linear-gradient(135deg, #e6d4f9, #ede0ff);
    }

    @keyframes float {
      0% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
      100% { transform: translateY(0); }
    }
  </style>
</head>
<body>

  <a href="/listings" class="landing-hero">
    <i class="fa-solid fa-plane-departure plane-icon"></i>
    <h1>Vista</h1>
    <p>Where Life Meets Travel</p>
    <button class="cta-button">Explore Now</button>
  </a>

</body>
</html>
